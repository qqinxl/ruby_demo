<!DOCTYPE html>
<html>
<head>
  <title>Blog & Life</title>
  <%= stylesheet_link_tag "application" %>
  <%= csrf_meta_tag %>
</head>
<body>
  <div id="container">
    <div id="box">
      <h1 id="sign"><%= link_to "#{t("sign.home.name", :default => "Blog & Life")}", root_path  %></h1>
      <%= yield %>
    </div>
  </div>
</body>
</html>
